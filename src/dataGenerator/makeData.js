let len = (range) => {
    const arr = []
    for (let i = 0; i < range; i++) {
        arr.push[i]
    }

    return arr
}


const colorSet = ['red', 'orange', 'yellow', 'green', 'blue', 'indigio', 'violet'],
const boolset = ['true', 'false']

const newPerson = () => {
    return {
        firstName: 'Test',
        lastName: 'Test2',
        age: 10,
        color: ['red']
    }
}

function makeData(...len) {

}